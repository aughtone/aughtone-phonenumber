[versions]
namespace = "io.github.aughtone.phonenumber"
group = "io.github.aughtone"
versionName = "0.0.1-alpha1"

# Byte-stability epoch: the pinned libphonenumber metadata release this port
# embeds. A bump here is a NEW normalization epoch, never an in-place change —
# downstream freezes token output per epoch. Exposed at runtime as a readable
# value. See docs/EPOCH.md.
libphonenumberMetadata = "9.0.38"

android-minSdk = "24"
android-compileSdk = "37"

agp = "9.2.1"
kotlin = "2.4.0"
kotlinx-serialization = "1.11.0"
kotlinx-datetime = "0.8.0"
vanniktech = "0.37.0"
kotlinBrowser = "2026.7.1"

[libraries]
kotlin-browser = { module = "org.jetbrains.kotlin-wrappers:kotlin-browser", version.ref = "kotlinBrowser" }
kotlin-test = { module = "org.jetbrains.kotlin:kotlin-test", version.ref = "kotlin" }
kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "kotlinx-serialization" }
kotlinx-datetime = { module = "org.jetbrains.kotlinx:kotlinx-datetime", version.ref = "kotlinx-datetime" }

[plugins]
multiplatformLibrary = { id = "com.android.kotlin.multiplatform.library", version.ref = "agp" }
kotlinMultiplatform = { id = "org.jetbrains.kotlin.multiplatform", version.ref = "kotlin" }
kotlinSerialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
vanniktech-mavenPublish = { id = "com.vanniktech.maven.publish", version.ref = "vanniktech" }
