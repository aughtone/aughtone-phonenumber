name: Publish

on:
  push:
    branches: [ "master" ]
  release:
    types: [published]

env:
  GRADLE_OPTS: -Dorg.gradle.jvmargs="-Xmx4g -XX:MaxMetaspaceSize=512m -XX:+UseParallelGC"

permissions:
  contents: write
  pull-requests: write

jobs:
  create-release:
    name: Create GitHub Release
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.VERSION }}
      tag_exists: ${{ steps.check_tag.outputs.exists }}
    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version
        id: get_version
        run: |
          VERSION=$(grep "versionName =" gradle/libs.versions.toml | cut -d'"' -f2)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - name: Check if tag exists
        id: check_tag
        run: |
          if git ls-remote --tags origin | grep -q "refs/tags/v${{ steps.get_version.outputs.VERSION }}$"; then
            echo "Tag v${{ steps.get_version.outputs.VERSION }} already exists. Skipping release."
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "Tag v${{ steps.get_version.outputs.VERSION }} does not exist. Proceeding with release."
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Create Release
        if: steps.check_tag.outputs.exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.get_version.outputs.VERSION }}
          name: Release ${{ steps.get_version.outputs.VERSION }}
          draft: false
          prerelease: false
          generate_release_notes: true
          target_commitish: master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish:
    name: Build, Package, and Publish
    needs: create-release
    if: |
      always() && (
        github.event_name == 'release' ||
        (github.event_name == 'push' && 
         needs.create-release.result == 'success' && 
         needs.create-release.outputs.tag_exists == 'false')
      )
    runs-on: macOS-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 21

      - name: Get version
        id: get_version
        run: |
          VERSION=$(grep "versionName =" gradle/libs.versions.toml | cut -d'"' -f2)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - name: Build XCFramework
        run: ./gradlew :phonenumber:assembleAOPhoneNumberKitReleaseXCFramework --no-configuration-cache

      - name: Zip and Checksum XCFramework
        id: xcframework
        run: |
          cd phonenumber/build/XCFrameworks/release
          zip -r AOPhoneNumberKit.xcframework.zip AOPhoneNumberKit.xcframework
          CHECKSUM=$(shasum -a 256 AOPhoneNumberKit.xcframework.zip | awk '{ print $1 }')
          echo "CHECKSUM=$CHECKSUM" >> $GITHUB_OUTPUT
          mv AOPhoneNumberKit.xcframework.zip $GITHUB_WORKSPACE/

      - name: Update Package.swift and Commit
        run: |
          VERSION="${{ steps.get_version.outputs.VERSION }}"
          CHECKSUM="${{ steps.xcframework.outputs.CHECKSUM }}"
          
          # Replace version in URL
          sed -i '' -E "s/download\/v[^\/]+\/AOPhoneNumberKit\.xcframework\.zip/download\/v$VERSION\/AOPhoneNumberKit.xcframework.zip/" Package.swift
          # Replace checksum
          sed -i '' -E "s/checksum: \"[^\"]+\"/checksum: \"$CHECKSUM\"/" Package.swift
          
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add Package.swift
          
          # Only commit and push if there are changes
          if ! git diff --staged --quiet; then
            git commit -m "chore: Update Package.swift checksum for v$VERSION [skip ci]"
            git push origin master
          fi

      - name: Upload XCFramework to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.get_version.outputs.VERSION }}
          files: AOPhoneNumberKit.xcframework.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Sync master back into develop
        run: |
          VERSION="${{ steps.get_version.outputs.VERSION }}"

          # Bring master's tip — the automated Package.swift checksum commit plus the
          # release merge — straight onto develop, so develop stays at master + new work
          # with no manual step. develop is unprotected, so the bot can push to it; the
          # develop push runs test.yml, and Publish only triggers on master so there is
          # no re-publish loop.
          git fetch origin develop:develop
          git checkout develop
          git merge master -m "Merge master into develop after v$VERSION release"
          git push origin develop
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to MavenCentral
        run: ./gradlew publishToMavenCentral --no-configuration-cache
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyId: ${{ secrets.SIGNING_KEY_ID }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.SIGNING_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.GPG_KEY_CONTENTS }}
